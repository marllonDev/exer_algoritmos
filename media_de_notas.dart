//Calcule a média de duas notas e mostre a média aritmética obtida.
void main() {
  double nota1 = 6.7;
  double nota2 = 9.9;
  //aqui somei as duas notas pq preciso saber o total primeiro para depois dividir pela quantidade individual de notas, neste caso eram duas notas. ENtão dividi simplesmente por 2.
  var total = nota1 + nota2;
  print(total);
  print(total / 2);
}
